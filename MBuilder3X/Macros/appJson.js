({
    ///////////////////////////////////////////////////////////////////////////
    // $appJson     - загрузка json (используется для подстановки загуженного json в макро)
    // $data
    //  url   - url
    ///////////////////////////////////////////////////////////////////////////
    $appJson: function (p) {
        let json = m.main.getAppJson(p.name, {}, p.folder);
        return json;
    }
});
